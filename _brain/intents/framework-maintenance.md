# Intent: Framework Maintenance

For changes to the AI Nexus source repository, load root `FRAMEWORK_STATE.md`, `README.md`, and only the affected installer, template, or tool files. Do not treat installed-project template state as the source repository's state. Avoid unrelated template folders and historical Git details unless the change requires them.
