# 2026-09-23

- Owner confirmed the cPanel domain and database/user are already created; focus is the upload file.
- Reorganized app into frontend/backend/database, retaining existing public PHP routes and adding Apache private-path protections.
- Added configurable initial Super Admin password used by CLI provisioning for new account only; existing accounts are preserved.
- Produced `dist/whittles-cpanel-upload.zip`, 51 runtime files; excludes `_brain` and local/dev-only content.
- Verified PHP syntax, 175 browser checks, desktop/mobile visual checks, Apache routes and denied private config/schema requests.
- Refactor and package source were committed/pushed as `a47882f`; `_brain` is in GitHub only and remains outside the ZIP.
- Tested the live URL with all five provisioned accounts and role permissions. Changed first-login passwords for TL and the three Management accounts to a temporary shared test password; Super Admin authenticated with its supplied password. Owner must rotate the four temporary passwords; original cPanel credential output is stale for those accounts.
- Created one clearly labeled QA employee (ID 1, later marked Exited) and ten uniquely tagged QA tickets (IDs 1–10, all closed), because no staff or tickets existed and ticket creation requires a linked employee. Attachment access and HTML/CSV reporting passed.
- Found ticket deletion is absent from the live UI/handler despite the schema permission/column. Safe delete-action probe did not delete anything; `frontend/ticket.php` is the fix target. QA rows remain for review. Evidence is `.local/qa/prod-live-audit-2026-09-23.md` (ignored/private).
- Next: owner rotates passwords; if requested, implement/test soft deletion and prepare a replacement ZIP. Avoid removing QA rows manually until a reviewed cleanup route exists.
