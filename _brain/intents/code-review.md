# Intent: Code Review

Load `CURRENT_STATE.md`, `sessions/LATEST_HANDOFF.md`, the submitted diff or explicitly named files, `governance/code_review_rules.md`, and `skills/code_review_checklist.md`. Load `fixes/fix_log.md` only to avoid duplicate findings. Avoid unrelated source files, old daily logs, and architecture history unless a finding requires them.
